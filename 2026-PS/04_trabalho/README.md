# 🎯 Autoavaliação

**Conceito pretendido:** A

**Justificativa:**

* **Ex. 1 e 2:** `calcularMedia()` e `contarAprovados()`.
* **Ex. 3:** `adicionarProduto()` e `listarProdutos()`.
* **Ex. 4:** `maiorValor(int[])` e `maiorValor(int, int)`.
* **Ex. 5:** `exibirBoletim()` reutiliza `calcularMedia()` e `contarAprovados()`.
* **Autoavaliação:** atividades concluídas conforme os exercícios propostos.




git push

